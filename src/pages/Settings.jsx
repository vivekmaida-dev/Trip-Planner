import Card from '../components/ui/Card'
import { homeCities, useSettingsStore } from '../store/settingsStore'

export default function Settings(){
 const { homeCity, defaultCurrency, preferences, updateSettings, updatePreferences } = useSettingsStore()
 return <div className='space-y-4'><Card><h2 className='section-title'>Settings</h2><div className='grid gap-2 sm:grid-cols-2'><label><span className='text-xs text-text-secondary'>Home city</span><select className='input-base' value={homeCity} onChange={(e)=>updateSettings({ homeCity:e.target.value })}>{homeCities.map((c)=><option key={c}>{c}</option>)}</select></label><label><span className='text-xs text-text-secondary'>Default currency</span><input className='input-base' value={defaultCurrency} onChange={(e)=>updateSettings({ defaultCurrency:e.target.value })}/></label><label><span className='text-xs text-text-secondary'>Default group size</span><input className='input-base' type='number' value={preferences.defaultGroupSize} onChange={(e)=>updatePreferences({ defaultGroupSize:Number(e.target.value) })}/></label><label><span className='text-xs text-text-secondary'>Preferred transport</span><select className='input-base' value={preferences.preferredTransportMode} onChange={(e)=>updatePreferences({ preferredTransportMode:e.target.value })}>{['car','flight','train','bus','other'].map((m)=><option key={m}>{m}</option>)}</select></label><label><span className='text-xs text-text-secondary'>Default hotel tier</span><select className='input-base' value={preferences.defaultHotelTier} onChange={(e)=>updatePreferences({ defaultHotelTier:e.target.value })}>{['budget','mid','premium','luxury'].map((t)=><option key={t}>{t}</option>)}</select></label><label className='flex items-center gap-2 pt-6'><input type='checkbox' checked={preferences.notifications} onChange={(e)=>updatePreferences({ notifications:e.target.checked })}/> Notifications (informational)</label></div><p className='mt-3 text-xs text-text-secondary'>Exchange rates are approximate and can be manually overridden during trip planning.</p></Card></div>
}
